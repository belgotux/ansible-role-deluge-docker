galaxy_info:
  role_name: deluge-docker
  author: belgotux
  description: Role to create deluge user/group and directory to use in the cluster. Based on the image `linuxserver/deluge`.
  license: GPL-3.0-or-later

  min_ansible_version: 2.1

  platforms:
  - name: Debian
    versions:
    - all
  - name: Ubuntu
    versions:
    - all

  galaxy_tags: 
  - docker
  - deluge

collections:
  - community.docker

dependencies: []